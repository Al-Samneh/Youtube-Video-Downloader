[project]
name = "samnehs-youtube-download"
version = "0.1.0"
description = "Local YouTube downloader with a companion Chrome extension."
requires-python = ">=3.10"
dependencies = [
  "imageio-ffmpeg>=0.6.0",
  "yt-dlp>=2026.1.1",
]

[tool.uv]
package = false
