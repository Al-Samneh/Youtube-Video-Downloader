[project]
name = "youtube-video-downloader"
version = "0.1.0"
description = "Local web app for downloading YouTube videos with subtitles."
requires-python = ">=3.10"
dependencies = [
  "imageio-ffmpeg>=0.6.0",
  "yt-dlp>=2026.1.1",
]

[tool.uv]
package = false
